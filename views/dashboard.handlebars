{{#each posts as |post| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <!-- Render user-specific data with Handlebars.js expressions -->
    <h2>{{post.title}}</h2>
  </div>
  <br>
  <div class="col-md-6">
    <h3>{{post.content}}</h3>
  </div>

</div>
{{/each}}
